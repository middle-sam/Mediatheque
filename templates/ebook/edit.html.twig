{% extends 'base.html.twig' %}

{% block title %}Edit Ebook{% endblock %}

{% block body %}
    <h1>Edit Ebook</h1>

    {{ include('ebook/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('ebook_index') }}">back to list</a>

    {{ include('ebook/_delete_form.html.twig') }}
{% endblock %}
