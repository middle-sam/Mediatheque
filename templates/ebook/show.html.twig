{% extends 'back_office/index.html.twig' %}

{% block title %}Ebook{% endblock %}

{% block body %}
    <h1>Ebook</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ebook.id }}</td>
            </tr>
            <tr>
                <th>Cote</th>
                <td>{{ ebook.cote }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ ebook.titre }}</td>
            </tr>
            <tr>
                <th>Format</th>
                <td>{{ ebook.format }}</td>
            </tr>
            <tr>
                <th>CodeOeuvre</th>
                <td>{{ ebook.codeOeuvre }}</td>
            </tr>
            <tr>
                <th>Pages</th>
                <td>{{ ebook.pages }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ebook_index') }}">back to list</a>

    <a href="{{ path('ebook_edit', {'id': ebook.id}) }}">edit</a>

    {{ include('ebook/_delete_form.html.twig') }}
{% endblock %}
